package uk.ac.cam.cl.interaction_design.group19.app.util;

/**
 * Interface for all app elements that should be updatable
 * whether because the location was changed or some time has passed
 */
public interface Updatable {
    void update();
}
